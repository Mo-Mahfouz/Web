تمام!
دلوقتي أنا هتصرّف **كأني عميل حقيقي** بطلب منك موقع ويب، وأعطيك **متطلبات كاملة** زي اللي الشركات بتبعتها للمبرمجين (Website Requirements Document).

بعد ما تخلص الموقع، ابعتهولي وأنا هقيّمه تقييم احترافي زي عميل أو Team Leader.

---

# 🧑‍💼 **طلب العميل (المتطلبات الرسمية للموقع)**

## 🎯 **اسم المشروع:**

**Mahfouz Books Hub**

## 🧾 **وصف عام للموقع:**

أريد موقع ويب بسيط بـ **HTML فقط** (بدون CSS أو JavaScript) يعرض معلومات عن الكتب، ويحتوي على صفحة رئيسية، وصفحة لعرض الكتب، وصفحة تواصل معي.

---

# 📄 **الصفحات المطلوبة:**

---

## 1️⃣ **الصفحة الرئيسية – index.html**

### تحتوي على:

* عنوان رئيسي للموقع `<h1>`

* فقرة تعريف بالموقع
* صورة مناسبة (أي صورة تضعها من الإنترنت)
* قائمة تنقّل `<nav>` تحتوي على:

  * الرئيسية
  * الكتب
  * تواصل
* استخدام عناصر:

  * `<strong>` و `<em>` و `<mark>`
  * `<br>` و `<hr>`
  * `<span>` داخل فقرة
* جزء (قسم) عن "أفضل الكتب" يحتوي على:

  * قائمة غير مرتبة `<ul>`
  * صورة لكل كتاب (3 كتب)
* جزء `<footer>` فيه:

  * حقوق النشر
  * لينك للاتصال بالإيميل (mailto)

---

## 2️⃣ **صفحة الكتب – books.html**

### تحتوي على:

* عنوان الصفحة
* جدول `<table>` يعرض 5 كتب:

  * اسم الكتاب
  * المؤلف
  * سنة النشر
  * صورة غلاف
* استخدام `<thead>`, `<tbody>`, `<tfoot>`
* استخدام `<figure>` و `<figcaption>` لكتاب واحد
* فقرة فيها:

  * `<q>`
  * `<blockquote>`
* زر `<button>` (بدون وظائف JS)

---

## 3️⃣ **صفحة التواصل – contact.html**

### يجب أن تحتوي على **فورم كامل** فيه:

* الاسم (text)
* البريد (email)
* كلمة المرور (password)
* اختيار نظام التشغيل (radio)
* اختيار هوايات (checkbox)
* اختيار مدينة من قائمة `<select>`
  مع استخدام `<optgroup>`
* رفع ملف
* مربع نص `<textarea>`
* إدخال تاريخ (date)
* إدخال وقت (time)
* اختيار لون
* حقل مخفي `<input type="hidden">`
* زر إرسال و زر إعادة تعيين
* استخدام:

  * placeholder
  * required
  * disabled
  * readonly

---

## 4️⃣ **ملفات إضافية**

### صفحة extra.html سيتم عرضها داخل iframe في الصفحة الرئيسية.

### تحتوي على:

* فقرة نصية
* استخدام `<code>`
* استخدام `<pre>`
* استخدام `<wbr>` و `<bdi>`

---

# 🎵 **وسائط Multimedia مطلوبة:**

في الصفحة الرئيسية:

* ملف صوت `<audio>`
* ملف فيديو `<video>`
  مع 2 `<source>` لكل واحد

(ممكن تضع أي روابط موجودة على الإنترنت)

---

# 💡 **شروط هامة جداً:**

* استخدام Semantic Elements قدر الإمكان:
  `<header>, <nav>, <section>, <article>, <footer>`
* الموقع يكون منظم وواضح
* الروابط بين الصفحات تعمل
* ممنوع استخدام CSS أو JS — HTML فقط

---

# ✔️ **بعد ما تخلص الملفات:**

ارسل:

1. **كل صفحات HTML الأربع**
2. **وأنا هقيّم المشروع بالكامل كعميل محترف**
3. وأعطيك ملاحظات تحسين كما يفعل مدير فريق Front-End

---

لو جاهز… قولّي:
**"ابدأ"**
عشان تبدأ تنفيذ المشروع.
